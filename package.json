{
  "name": "avica-2.0",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"python backend/manage.py runserver\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "run": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
